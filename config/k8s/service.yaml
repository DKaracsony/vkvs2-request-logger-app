apiVersion: v1
kind: Service
metadata:
  name: exam-app-service
  namespace: exam-david-karacsony
spec:
  type: NodePort
  selector:
    app: exam-app
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
      nodePort: 30082